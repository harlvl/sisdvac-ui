export interface Trial {
  title: string;
  stage: string;
  phase: string;
  insNumber: string;
  status: boolean;
  researchEntity: string;
  startDate: string;
  endDate: string;
  advances: any;
}
